import { StyleSheet, Text, View, Image, ScrollView, Button } from 'react-native'
import React from 'react'

export default function EkipDetay({props}) {
  return (
    <View>
      <Text>asda</Text>
    </View>
  )
}

const styles = StyleSheet.create({
    container:{
        paddingBottom: 100,
    },
    img:{
        width: '100%',
        height: 300,
        padding: 200,
    },
    name:{
        fontSize: 25,
        fontWeight: 'bold',
        textAlign: 'center'
    }
})